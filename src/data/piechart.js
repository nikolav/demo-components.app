import d3 from "d3";
import { merge } from "../util"
////
const DEFAULT_CONFIG = {
  width: 320,
  height: 320,
  innerRadius: .5,
  colors: d3.scaleOrdinal(d3.schemeCategory10),
};
export default function piechart(canvas, data, config = {}) {

} 
