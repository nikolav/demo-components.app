import LayoutMain from "../components/layout/LayoutMain";

const DemoAccordion = (props) => (
  <LayoutMain>
    <section>acc.1</section>
    <section>acc.2</section>
  </LayoutMain>
);

export default DemoAccordion;
